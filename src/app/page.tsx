import { Metadata } from "next";
import Main from "@/components/main";

export const metadata: Metadata = {
  title: "Home | Imagine Festival",
};

export default function Home() {
  return <Main />;
}
